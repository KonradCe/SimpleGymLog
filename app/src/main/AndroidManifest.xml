<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.kcworks.simplegymlog">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--TODO[3]: this is an excuse of a solution. It blocks screen rotation so changes done to the exercise while editing won't reset-->
        <activity android:name=".ui.AddExerciseActivity"
            android:screenOrientation="portrait">
        </activity>
        <activity android:name=".ui.WorkoutActivity" />
        <activity android:name=".ui.WorkoutPickerActivity" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>